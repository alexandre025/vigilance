<% content_for :header do %>
    <%= render partial: 'shared/header', locals: {current: :saved_contents} %>
<% end %>

<h2><%= t('saved_to_read') %></h2>

<%= render partial: 'shared/contents', locals: {contents: current_user.contents_saveds.where(relation: :to_read).map { |c| c.content }} %>