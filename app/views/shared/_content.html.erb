<% if content %>
  <div id="content_<%= content.id %>" class="grid-item">
    <a href="<%= organization_content_path(content.organization, content) %>">
      <%= content.title %>
    </a>
  </div>
<% end %>