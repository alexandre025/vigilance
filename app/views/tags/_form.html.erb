<%= form_for ([@organization,tag]) do |f| %>
  <% if tag.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(tag.errors.count, "error") %> prohibited this tag from being saved:</h2>

      <ul>
      <% tag.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <%= f.text_field :name, placeholder: t('placeholder.new_tag.name') %>
    <%= f.submit (tag.new_record? ? t(:tag_new_submit) : t(:tag_edit_submit)), class: 'after-input' %>
  </div>

<% end %>
