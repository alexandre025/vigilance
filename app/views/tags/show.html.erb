<% content_for :header do %>
    <%= render partial: 'shared/header', locals: {current: :tags} %>
<% end %>

<h2><%= "#{@organization.name} > #{@tag.name}" %></h2>

<%= render partial: 'shared/contents', locals: {contents: @tag.contents} %>

<%= link_to 'Edit', edit_organization_tag_path(@organization,@tag) if can? :update, @tag %> |
<%= link_to 'Back', organization_tags_path(@organization) %>
